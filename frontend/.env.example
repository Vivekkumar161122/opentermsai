# OpenTerms AI Frontend Configuration
# Optional: Set this to your deployed FastAPI backend URL (e.g. on Railway, Render, or GCP Cloud Run)
# If omitted or unreachable, OpenTerms AI seamlessly operates in Interactive Standalone Mode with complete pre-computed sample contract reasoning!
NEXT_PUBLIC_API_URL=http://localhost:8000
